const Models = require('./models');

module.exports = {
  messages: {
    get: (request, response) => {
      // do something
    },
    post: (request, response) => {
      // do something else
    }
  }
};
